{
  "name": "small_capital_10k_forward_paper",
  "experiment_id": "small_capital_10k",
  "description": "Small Capital Paper: ¥10,000 initial capital with integer share execution. Reuses frozen FINAL model; does not alter 10M paper state.",
  "extends": "config/true_holdout.json",
  "immutable_strategy": true,
  "country": "United States",
  "initial_capital": 10000,
  "forward": {
    "start": "2026-08-01",
    "locked": true,
    "rationale": "Same forward start as 10M paper. Isolated state under data/paper_experiments/small_capital_10k."
  },
  "model_freeze": {
    "retrain_forbidden": true,
    "purge_days": 5,
    "early_stopping_valid_days": 252,
    "note": "Reuse frozen model paper_4ab12cb5ade1681f. Capital change must not trigger retrain."
  },
  "portfolio": {
    "max_position_weight": 1.0,
    "note": "SC sizing is cash/affordability-aware; weight=1.0 avoids 20% cap blocking all integer lots at ¥10k. max_positions from FINAL still applies."
  },
  "execution_policy": {
    "share_mode": "integer",
    "minimum_quantity": 1,
    "allow_fractional": false,
    "sizing_mode": "integer_affordable",
    "min_lot_overrides_weight": true
  },
  "data_freshness": {
    "max_stale_calendar_days": 5,
    "reject_stale_for_orders": true
  },
  "brokerage": {
    "enabled": false,
    "note": "Paper trading only. No real orders."
  },
  "state": {
    "default_dir": "data/paper_experiments/small_capital_10k",
    "forbid_canonical_10m_dir": true
  }
}
